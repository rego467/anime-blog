import { Inter, Kanit, Genos,Tajawal, Lato } from "next/font/google";

export const inter = Inter({
  variable: '--font-inter',
  subsets:['latin'],
  weight:"700"
})

export const kanit = Kanit({
  variable: '--font-kanit',
  subsets:['latin'],
  weight:"900"
})

export const genos = Genos({
  variable:'--font-genos',
  subsets:['latin'],
  weight:'600'
})

export const tajawal = Tajawal({
  variable: '--font-tajawal',
  subsets:['latin'],
  weight: '400'
})

export const lato = Lato({
  variable: '--font-lato',
  subsets:['latin'],
  weight:'900'
})